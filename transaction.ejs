        <% mdata.forEach(item => { %>   
        <tr>
            <th><%= item.id %></th>
            <th><%= item.title %></th>
            <th><%= item.price %></th>
            <th><%= item.description %></th>
            <th><%= item.category %></th>
            <th><%= item.image %></th>
            <th><%= item.sold %></th>
            <th><%= item.dateOfSale %></th>
            

        </tr>
        <%})%>  